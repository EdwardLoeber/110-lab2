let snacks: string[] = ['Turbos', 'Popcorn', 'Sour candy', 'Chocolate', 'Cookies'];

function printSnacks(snacks: string[]): void {
    snacks.forEach(snack => console.log(snack));
}

printSnacks(snacks);